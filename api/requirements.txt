fastapi>=0.115
uvicorn[standard]>=0.30
pymupdf>=1.24
python-multipart
httpx
pydantic>=2.0
scikit-learn>=1.4
numpy>=1.26
playwright>=1.45
browser-use>=0.1
openai>=1.30          # OpenAI-compatible client (used for both Ollama + Groq)
langchain-ollama>=0.2 # Ollama LangChain integration (browser agent)
# Keep groq + langchain-groq if switching back via LLM_BACKEND=groq
groq>=0.9
langchain-groq>=0.2
